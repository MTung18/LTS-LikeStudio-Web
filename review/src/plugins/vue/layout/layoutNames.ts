// In order to prevent a circular reference, which makes enum values undefined at runtime, these must be in their own file

export const enum LayoutNames {
  DEFAULT = "default",
  EMPTY = "empty",
}
